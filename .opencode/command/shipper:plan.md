---
description: Plan the implementation of a specific ticket.
agent: plan
---

Plan the implementation of $ARGUMENTS. Use the linear tool to retrieve the ticket.

Before planning, make sure to change to the main branch and pull the latest changes from origin into it. Then create a new feature branch based on the discussed ticket in linear. Afterwards set the ticket to "in Progress" using the linear tool.
