steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-f', 'cmd/controller/Dockerfile', '-t', '$LOCATION-docker.pkg.dev/$PROJECT_ID/metio/controller:$COMMIT_SHA', '.']
images:
- '$LOCATION-docker.pkg.dev/$PROJECT_ID/metio/controller:$COMMIT_SHA'
options:
  logging: CLOUD_LOGGING_ONLY
